# Instrucciones para Crear tu Firma de Correo

Sigue estos pasos para generar y configurar tu firma de correo electrónico profesional de Global Reborn Group.

### Paso 1: Abrir el Editor en Línea
Entra a la siguiente página para poder editar y previsualizar tu firma en tiempo real:
👉 [OneCompiler - HTML Editor](https://onecompiler.com/html/44d8383aa)

### Paso 2: Personalizar tu Información
1. En el panel izquierdo del editor, verás el código HTML.
2. Busca los siguientes campos y cambia el texto entre corchetes por tu información real (mantén las etiquetas en inglés como `Name:`, `Position:`, etc.):
   - `Name`: Reemplaza `[FULL NAME]` por tu nombre completo.
   - `Position`: Reemplaza `[JOB POSITION]` por tu cargo oficial.
   - `Contact`: Reemplaza `[COUNTRY CODE]` (ej: +591) y `[PHONE NUMBER]` por tu teléfono.
   - `Email 1`: Reemplaza `email1@grb-group.com` (en el enlace `href`) y `[EMAIL 1]` (en el texto) por tu correo principal.
   - `Email 2`: Reemplaza `email2@grb-group.com` (en el enlace `href`) y `[EMAIL 2]` (en el texto) por tu correo secundario.

### Paso 3: Copiar el Resultado
Una vez hayas editado tu información, verás el diseño final en el panel derecho (Previsualización).
1. Selecciona todo el contenido visual de la **derecha** (la firma diseñada).
2. Dale clic derecho y selecciona **Copiar** (o usa `Ctrl + C`).

### Paso 4: Configurar en Gmail
1. Abre tu Gmail y ve a **Configuración** (el ícono de engranaje ⚙️) > **Ver toda la configuración**.
2. Desplázate hacia abajo hasta encontrar la sección **Firma**.
3. Haz clic en **Crear nueva** o selecciona la firma que ya tienes.
4. En el cuadro de texto de la derecha, **pega** la firma que copiaste anteriormente (`Ctrl + V`).
5. Ve hasta el final de la página y haz clic en el botón **Guardar cambios**.

---
*Nota: Asegúrate de revisar que los enlaces de correo funcionen correctamente antes de guardar los cambios.*
